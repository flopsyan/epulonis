<article class="card recipe-card">
  <a class="card-media" href="/recipe/<%= recipe.slug %>">
    <span class="card-fallback" aria-hidden="true"><%= (recipe.title || '?').trim().charAt(0).toUpperCase() %></span>
    <% if (recipe.image_url) { %>
      <img
        src="<%= recipe.image_url %>"
        alt="<%= recipe.title %>"
        loading="lazy"
        onerror="this.remove()"
      />
    <% } %>
    <span class="card-badge"><%= t('diff_' + recipe.difficulty) %></span>
  </a>

  <div class="card-body">
    <% if (recipe.tags && recipe.tags.length) { %>
      <div class="chips chips-sm">
        <% recipe.tags.slice(0, 3).forEach((tag) => { %>
          <a class="chip" href="/?tag=<%= tag.slug %>"><%= tag.name %></a>
        <% }) %>
      </div>
    <% } %>

    <h3 class="card-title"><a href="/recipe/<%= recipe.slug %>"><%= recipe.title %></a></h3>

    <% if (recipe.description) { %>
      <p class="card-desc"><%= recipe.description %></p>
    <% } %>

    <div class="card-meta">
      <% const total = (recipe.prep_time || 0) + (recipe.cook_time || 0) + (recipe.rest_time || 0); %>
      <% if (formatTime(total)) { %><span title="<%= t('total_time') %>"><%- include('icon', { name: 'clock' }) %> <%= formatTime(total) %></span><% } %>
      <span title="<%= t('servings_label') %>"><%- include('icon', { name: 'servings' }) %> <%= formatAmount(recipe.servings) %> <%= recipe.servings_unit %></span>
    </div>
  </div>
</article>
